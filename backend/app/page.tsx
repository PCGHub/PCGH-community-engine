/**
 * Minimal root page. Required by Next.js App Router for the application
 * to boot. No business logic -- this exists only to satisfy Phase 5
 * Step 2's "infrastructure boots without error" exit criterion. Real
 * routes/pages are implemented in later Phase 5 steps
 * (docs/phase-5-roadmap.md).
 */
export default function RootPage() {
  return null;
}
