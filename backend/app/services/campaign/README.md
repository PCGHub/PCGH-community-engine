# Campaign Service (Application)

Coordinates the Campaign, Discovery, and Protection domains for campaign publishing and rotation. Calls `api.distribute_campaign()`, `api.rotate_campaign()`, `api.close_campaign()`, `api.archive_campaign()`, `api.restore_campaign()`, `api.calculate_campaign_performance()`. See `docs/service-architecture.md` and `../README.md`.
