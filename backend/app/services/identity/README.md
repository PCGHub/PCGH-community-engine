# Identity Service (Domain)

User profiles, creator profiles, community membership, account lifecycle. Calls the profile fields of `api.creator_dashboard_view` and `api.member_dashboard_view`. See `docs/service-architecture.md` and `../README.md`.
