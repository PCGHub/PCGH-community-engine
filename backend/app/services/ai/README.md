# AI Service (Application)

AI orchestration and prompt management, integrating the separate Python/FastAPI AI Service (`ADR-016`) via a defined API contract — not a shared runtime. AI-assisted decision-making features remain out of scope pending ADR approval. See `docs/service-architecture.md` and `../README.md`.
