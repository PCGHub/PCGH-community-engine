# Analytics Service (Domain)

Dashboards, reporting, aggregated metrics. Calls `api.platform_statistics_view` (administrator-only) and `api.community_dashboard_view`. See `docs/service-architecture.md` and `../README.md`.
