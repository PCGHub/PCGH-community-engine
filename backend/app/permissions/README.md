# app/permissions/

Advisory, UX-level permission checks only. Row Level Security (already enforced in migrations 001-009) is the actual, authoritative authorization boundary — this layer never substitutes for it (`docs/authentication-architecture.md`, "Authorization Model").
