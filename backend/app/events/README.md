# app/events/

Event definitions matching `analytics.analytics_events`'s governed `event_type` vocabulary. New event types require extending that vocabulary via the Architecture Change Lifecycle, not adding one silently here (`docs/service-architecture.md`, "Events").
