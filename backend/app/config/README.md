# app/config/

Configuration and environment handling. The Supabase `service_role` key is server-side only and must never be referenced in client-reachable code (`docs/authentication-architecture.md`, "Security Principles"). Configuration Foundation is Phase 5 Step 2 (`docs/phase-5-roadmap.md`) — no configuration logic is implemented in this scaffold.
