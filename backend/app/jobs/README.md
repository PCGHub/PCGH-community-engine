# app/jobs/

Background/asynchronous processing (campaign distribution, cooldown expiration, bonus processing, analytics aggregation, notifications, AI processing). Must remain idempotent and retry-safe (`docs/backend-architecture.md`, "Background Jobs").
