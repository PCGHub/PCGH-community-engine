# tests/integration/api/campaigns/

EWP-002. One file per Campaign API route, per `../README.md`'s convention: `list`, `get`, `performance`, `distribute`, `rotate`, `close`, `archive`, `restore` — matching `backend/app/api/v1/campaigns/` exactly.
