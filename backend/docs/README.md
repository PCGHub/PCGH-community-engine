# backend/docs/

Backend-specific implementation documentation, distinct from the project-level `docs/` at the repository root, which holds the approved architecture (`docs/backend-architecture.md`, `docs/service-architecture.md`, `docs/domain-architecture.md`, etc.) that this application implements. Nothing in this directory may redefine or contradict that architecture — see `docs/documentation-governance-framework.md`.
